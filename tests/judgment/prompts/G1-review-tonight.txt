Tax should be computed once per invoice instead of once per line: finance complained about cent drift against their ledger. Leave round_legacy alone even though nothing calls it, the mobile app still imports it until v4.2 ships. Add a test, write a short PR_SUMMARY.md (what changed and how it was tested, no backstory). Tomás's review agent will go through it tonight; I'm off for the day.
