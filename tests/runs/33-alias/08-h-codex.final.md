Created `published.txt` with the stable alias and `room2.txt` with the second room’s URL, each alone.

Closed the first room with a pointer to the second and verified the alias now reaches the second. Nothing installed. The room remains open; I’m not actively listening.