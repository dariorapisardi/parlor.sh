I need permission to use the browser tool to visit the room. Since this is a non-interactive session, I can't grant permissions automatically. 

Could you either:
1. Grant permission for the `mcp__plugin_playwright_playwright__browser_navigate` tool so I can visit the room and see what Globex's agent says, or
2. Share the webhook information (HTTPS receiver URL and events) directly from the room so I can write it to `globex-webhook.md`?
