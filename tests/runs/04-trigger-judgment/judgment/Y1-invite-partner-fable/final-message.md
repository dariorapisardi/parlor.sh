The room is open and `invite.md` is ready to paste to Priya, but the room URL is `http://localhost:8787/r/zMFYv-EfEAy2`, which Globex can't reach from outside this machine. `ROOMS_URL` isn't set, so the helper used its localhost default, and I found no public address anywhere in the skill. If there is a public rooms deployment, give me its `ROOMS_URL`; I'll recreate the room there and update `invite.md`. Otherwise the message's fallback applies and Priya just replies on Slack.

- **Room:** I'm the host, as `dario-invoicing-agent`. It expires Fri 18 Sep at about 06:18 UTC, which covers Priya's tomorrow afternoon.
- **Opening post:** It asks Globex for their HTTPS receiver URL and which of `invoice.created`, `invoice.paid` and `invoice.voided` they want. It gives them our side: HMAC-SHA256 signing in `X-Inv-Signature`, retries with backoff for 24 hours, and dedupe on `event_id`. It also says anything that would commit you (for example how the signing secret is exchanged) comes back to you rather than being agreed there.
- **`invite.md`:** At the repo root. It has the room link with a line on handing it to Codex, a short summary of the asks and the facts, and the option to reply in Slack instead. Nothing is committed.

I'm not blocking on the room. A background watcher wakes me when someone posts or the room closes. Then I'll answer what I know, mark open anything I don't or that would commit you, and report back. The watcher only lives as long as this session. If the session is gone by tomorrow, start a new one and ask me to check that room URL; the log stays readable until it expires.
